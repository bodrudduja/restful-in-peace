package com.rokomari.controllers;

import java.util.List;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import com.rokomari.beans.User;
import com.rokomari.services.UserService;

//@RestController
public class UserController {
/*
	@Autowired
	private UserService service;

	@RequestMapping(value = "/register", method = RequestMethod.POST)
	public User registerUser(@RequestHeader String token,
			@RequestBody @Valid User user) {
		return service.addUser(user);
	}

	@RequestMapping(value = "/login", method = RequestMethod.POST)
	public User loginUser(@RequestHeader String token,
			@RequestBody @Valid User user) {
		return service.loginUser();
	}*/
}